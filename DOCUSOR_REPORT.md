# Docs Verified: ❌

**Step 1:** `docker compose up -d --build` — ❌ Failed

```stderr
unable to get image 'docusor-test-api:local': Cannot connect to the Docker daemon at unix:///var/run/docker.sock. Is the docker daemon running?

```